import 'package:flutter/material.dart';

class NuevoAnime extends StatelessWidget {
  const NuevoAnime({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Nuevo Anime"));
  }
}
